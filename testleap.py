import leap

# Test out your methods by calling them here. Remeber that because they are in a different file, you must reference them with a dot like "leap.is_leap_year(1997)".
# We've written two tests for your first method, so these two should work once you successfully code out the is_leap_year method.

print(leap.is_leap_year(1997))

print(leap.is_leap_year(1996))
